const initialState = {
  houseList: null,
};
const sachNhaReducer = (state = initialState, action) => {
  let { type, payload } = action;
  switch (type) {
    default:
      return state;
  }
};
export default sachNhaReducer;
